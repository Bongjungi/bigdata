package memo;

public class Equals {
	// object의 equals는 주소를 비교한다.
	// string의 equals는 주소 안의 값을 비교한다.
	// tostring은 object에서 상속받는다. 당연히 stringbuilder에서 쓰는 tostring이랑 다르다.
	// new StringBuilder(new String(new char[])).toString()

}
